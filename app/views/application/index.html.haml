%h2 Index

%p= link_to("Log In", user_login_path)
%p= link_to(" Sign Up", new_user_path)

-@buses.each do |bus|
    %p= link_to("#{bus.name}", business_path(bus))
-@users.each do |usr|
    %p= link_to("#{usr.name}", user_path(usr))